# Default values for file-distribution-operator.
# This is a YAML-formatted file.
# Declare variables to be passed into your templates.

installCRD: false

config:
  namespace: fdo-system
  prefix: fdo-
  image: # operator image comes here